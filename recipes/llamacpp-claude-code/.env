# llama.cpp 서버 설정
# MODEL_PATH: 다운로드한 GGUF 파일의 절대 경로
MODEL_PATH="${HOME}/models/qwen2.5-coder-7b-instruct-q4_k_m.gguf"
LLAMA_PORT=8080
CONTEXT_SIZE=4096
GPU_LAYERS=-1      # -1: 전체 레이어를 Metal(GPU)에 올림 / 0: CPU 전용

# LiteLLM 프록시 설정 (Anthropic API 변환용)
LITELLM_PORT=4000
PROXY_MODEL_NAME="my-model"
